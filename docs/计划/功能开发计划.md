# 用户登录 + Web界面开发计划

## 📁 新的模块结构

```
app/
├── agent/              # 🤖 智能体模块（不变）
│   ├── matcher.py
│   └── rule_agent.py
│
├── database/           # 💾 数据库模块（不变）
│   ├── db.py
│   ├── db_manager.py
│   └── models_db.py
│
├── auth/               # 🔐 用户认证模块（新增）
│   ├── __init__.py
│   ├── auth_service.py    # 认证服务（注册、登录、密码加密）
│   └── session_manager.py # 会话管理
│
├── web/                # 🌐 Web界面模块（新增）
│   ├── __init__.py
│   ├── routes.py          # Web路由（HTML页面）
│   ├── templates/         # HTML模板文件夹
│   │   ├── base.html
│   │   ├── login.html
│   │   ├── register.html
│   │   ├── index.html
│   │   ├── post_lost.html
│   │   ├── post_found.html
│   │   └── matches.html
│   └── static/            # 静态文件（CSS/JS）
│       ├── css/
│       │   └── style.css
│       └── js/
│           └── main.js
│
├── main.py            # Flask应用主入口（需要更新）
└── models.py          # 业务模型（需要添加密码字段）
```

## 📋 开发任务清单

### 1. 用户认证模块（app/auth/）
- [ ] 创建 `app/auth/` 文件夹
- [ ] `auth_service.py` - 实现：
  - 用户注册（密码加密）
  - 用户登录验证
  - 密码哈希（使用werkzeug.security）
- [ ] `session_manager.py` - 实现：
  - Flask session管理
  - 登录状态检查装饰器

### 2. 数据库模块扩展（app/database/）
- [ ] 更新 `db_manager.py` - 添加：
  - `create_user()` - 创建用户
  - `get_user_by_student_id()` - 根据学号查询用户
  - `get_user_by_id()` - 根据ID查询用户
- [ ] 更新 `models_db.py` - 确保UserDB有password_hash字段

### 3. Web界面模块（app/web/）
- [ ] 创建 `app/web/` 文件夹和子文件夹
- [ ] `routes.py` - 实现Web路由：
  - `/` - 首页
  - `/login` - 登录页面
  - `/register` - 注册页面
  - `/post_lost` - 发布失物页面
  - `/post_found` - 发布招领页面
  - `/matches/<lost_id>` - 查看匹配结果页面
  - `/logout` - 登出
- [ ] HTML模板（使用Jinja2）：
  - `base.html` - 基础模板（导航栏、布局）
  - `login.html` - 登录表单
  - `register.html` - 注册表单
  - `index.html` - 首页（失物/招领列表）
  - `post_lost.html` - 发布失物表单
  - `post_found.html` - 发布招领表单
  - `matches.html` - 匹配结果展示
- [ ] 静态文件：
  - `static/css/style.css` - 样式（使用Bootstrap）
  - `static/js/main.js` - 前端交互

### 4. 更新主程序（app/main.py）
- [ ] 整合API路由和Web路由
- [ ] 配置Flask（secret_key、session等）
- [ ] 注册Web路由蓝图

### 5. 更新业务模型（app/models.py）
- [ ] User模型添加password_hash字段（可选，因为数据库模型已有）

## 🎯 实现顺序

1. **先做认证模块** - 用户注册/登录功能
2. **再做Web界面** - HTML页面和路由
3. **最后整合** - 把API和Web路由整合到一起

## ✅ 完成标准

- [ ] 用户可以注册账号
- [ ] 用户可以登录/登出
- [ ] 登录后才能发布失物/招领
- [ ] 有完整的Web界面（不是只有API）
- [ ] 代码模块清晰（auth、web、agent、database分离）

